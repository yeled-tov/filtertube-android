<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- נגן מדיה מקומי: קריאת האודיו והווידאו שכבר על המכשיר.
         מאנדרואיד 13 ההרשאות מפוצלות לפי סוג מדיה; לפני כן אחת כללית. -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!--
          מחווה "חזרה חזויה" מכובה במפורש.

          דוח הקריסה מהמכשיר:
            IndexOutOfBoundsException: Index -1 out of bounds for length 1
              at androidx.navigation.compose.NavHost...
              at androidx.activity.compose.OnBackInstance$job$1 (PredictiveBackHandler)

          זו תקלה בתוך navigation-compose עצמה: כשמחליקים חזרה ביעד האחרון
          שנותר בערימה, היא מנסה לקרוא את הפריט שלפניו ומקבלת אינדקס ‎-1‎.
          אין לנו דרך לתפוס את זה מבחוץ — הקריסה קורית בתוך הספרייה, בקורוטינה
          שלה.

          כיבוי המחווה מחזיר את התנהגות ה"חזור" הרגילה, שעובדת. אין כאן ויתור
          על תכונה שהמשתמש מרוויח ממנה: האנימציה החזויה שווה הרבה פחות
          מאפליקציה שלא קורסת.
    -->
    <application
        android:name=".FilterTubeApp"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.FilterTube"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="false"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:supportsPictureInPicture="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.FilterTube">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- פתיחת קישורי יוטיוב באפליקציה (מופיע ב"פתח באמצעות") -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="www.youtube.com" />
                <data android:scheme="https" android:host="m.youtube.com" />
                <data android:scheme="https" android:host="youtube.com" />
                <data android:scheme="https" android:host="youtu.be" />
                <data android:scheme="http" android:host="www.youtube.com" />
                <data android:scheme="http" android:host="youtu.be" />
            </intent-filter>
        </activity>

        <!-- שירות ניגון ברקע + חלונית בהתראות/מסך נעילה -->
        <service
            android:name=".playback.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
    </application>
</manifest>
